# 游戏设定功能 - 修改说明

## 功能概述
在 Operit 左侧抽屉菜单「AI 功能」分类中新增「游戏设定」入口。
- 游戏介绍（必填）/ 游戏设定（可选）/ 人物设定（可选），支持手动输入或上传 txt
- 粘贴/上传游戏介绍后自动调用 AI 识别难度选项，展示在底部（单选）
- 未识别到难度时显示"读取失败，请手动选择难度"（可手动输入）
- 选择难度后点「下一步」进入游戏对话：仅允许上传 txt + 文字，可自由切换模型与思考程度，并自动开启最大上下文模式

## 修改/新增文件清单

### 修改文件
| 文件 | 改动 |
|---|---|
| `app/src/main/java/com/ai/assistance/operit/ui/common/NavItem.kt` | 新增 `GameSetup` 导航项（route="game_setup"，图标 SportsEsports） |
| `app/src/main/java/com/ai/assistance/operit/ui/main/OperitApp.kt` | `navItems` 列表加入 `NavItem.GameSetup` |
| `app/src/main/java/com/ai/assistance/operit/ui/main/screens/OperitScreens.kt` | 新增 `Screen.GameSetup`（填写页）与 `Screen.GameChat`（游戏对话页） |
| `app/src/main/java/com/ai/assistance/operit/ui/main/screens/ScreenRouteRegistry.kt` | 注册 `main.game_setup` 路由（surface=MAIN_SIDEBAR_AI），侧边栏高亮归属 NavItem.GameSetup |
| `app/src/main/java/com/ai/assistance/operit/ui/features/chat/components/AttachmentSelector.kt` | `AttachmentSelectorPanel`/`AttachmentSelectorPopupPanel` 新增 `restrictedToText` 参数，受限时仅保留"文件(text/plain)"按钮 |
| `app/src/main/java/com/ai/assistance/operit/ui/features/chat/components/style/input/agent/AgentChatInputSection.kt` | 新增 `restrictedToText` 参数并透传 |
| `app/src/main/java/com/ai/assistance/operit/ui/features/chat/components/style/input/classic/ClassicChatInputSection.kt` | 新增 `restrictedToText` 参数并透传 |
| `app/src/main/java/com/ai/assistance/operit/ui/features/chat/screens/AIChatScreen.kt` | 新增 `restrictedGameMode` 参数，透传给两个输入区 |
| `app/src/main/res/values/strings.xml` | 新增 `nav_game_setup=游戏设定` |
| `app/src/main/res/values-en/strings.xml` | 新增 `nav_game_setup=Game Setup` |

### 新增文件
| 文件 | 作用 |
|---|---|
| `app/src/main/java/com/ai/assistance/operit/ui/features/game/screens/GameSetupScreen.kt` | 游戏设定填写界面：三输入区 + 上传txt + 自动识别难度（调用 SUMMARY 功能 AI 服务）+ 难度选择 + 下一步 |
| `app/src/main/java/com/ai/assistance/operit/ui/features/game/screens/GameChatScreen.kt` | 游戏对话界面：复用 AIChatScreen（restrictedGameMode=true），进入时创建新对话、开启最大上下文、自动发送游戏初始化消息 |

## 实现要点
1. **难度识别**：`GameSetupScreen` 在游戏介绍文本变化后 800ms（防抖）调用 `aiRecognizeDifficulty`：
   - 通过 `MultiServiceManager.getServiceForFunction(FunctionType.SUMMARY)` 获取 AI 服务
   - 系统提示要求输出 `DIFFICULTY_LIST:\n- 难度1\n- 难度2...` 格式
   - 解析结果展示为单选列表；空结果 → 显示"读取失败，请手动选择难度"
2. **游戏对话入口**：GameSetup「下一步」→ `navigateTo(GameChat(...))`，GameChat 的 `Screen.navItem = NavItem.GameSetup`（侧边栏保持高亮游戏设定）
3. **受限输入**：`restrictedGameMode=true` → 附件面板只保留"文件"按钮且 MIME 为 text/plain，其余附件（图片/拍照/记忆/屏幕/通知/位置/包）隐藏；文字输入、模型切换、思考程度、上下文设置均保留
4. **上下文**：进入游戏对话自动 `toggleEnableMaxContextMode()` 开启最大上下文，防止玩久了忘记设定
5. **首次消息**：自动创建新对话并 `sendTextMessage(游戏介绍+游戏设定+人物设定+难度)`，AI 据此开始游戏

## 构建方式
使用项目原有构建流程（Android Studio 或 `./gradlew assembleDebug`）即可。若需同步其他语言资源，可在对应 `values-xx/strings.xml` 中添加 `nav_game_setup` 条目。
